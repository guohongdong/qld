<!--pages/order/order.wxml-->
<view class='order-container'>
  <block wx:for="{{orderList}}">
    <order-item-cpm order="{{item}}"></order-item-cpm>
  </block>
  <view class='no-order' wx:if="{{orderList.length==0}}">暂无订单</view>
</view>